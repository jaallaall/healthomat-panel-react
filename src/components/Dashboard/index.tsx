import { Button } from "@UI";

const Dashboard: React.FC = (): React.ReactElement => {
  return (
    <div>
      {/* <Button
        variant="outline"
        endIcon={<Icon name="logout" className="ms-4" />}
      >
        dosh
      </Button> */}
    </div>
  );
};

export default Dashboard;
